# Development Variants

This directory retains two alternate uploaded copies of the same segmentation implementation.

- `referenced-version/` adds explanatory comments and source references.
- `ascii-compatible-version/` replaces several Unicode punctuation characters with ASCII equivalents and includes a corresponding report copy.

These files are preserved for traceability. They are not merged into or substituted for the featured implementation in `src/hw1.py`.
